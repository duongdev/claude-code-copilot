node_modules/
*.log
.DS_Store
.env
.claude-copilot-auth.json

# Local runtime config (auth tokens, secrets). Keep the dir tracked via .gitkeep
# and check in *.example.json templates.
.config/*.json
!.config/*.example.json
